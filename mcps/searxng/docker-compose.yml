version: '3.8'

services:
  searxng:
    image: searxng/searxng:latest
    container_name: q-spec-searxng
    ports:
      - "8080:8080"
    volumes:
      - ./settings.yml:/etc/searxng/settings.yml:ro
    restart: unless-stopped
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080/
